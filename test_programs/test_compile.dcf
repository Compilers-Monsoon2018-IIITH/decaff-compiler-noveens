class Program {
	int arr[ 100 ];
	int a, b, c;

	int temp(int aa, int bb) {
		if (aa % 2 == 0) {
			b = 2;
			return 0;
		}
		
		// if (aa % 2 == 1) {
		else {
			b = 2;
			return 1;
		}
		
		return -1;
	}

	int main() {
		int a;
		int n;
		int mx;
		boolean tmp;
		n += 5;
		tmp = n > 0;
		callout("printf", "n = %d and tmp = %d; ", n, tmp);
		callout("printf", "temp(2) = %d, temp(1) = %d; ", temp(2, 0), temp(1, 0));
		
		temp(1, 2);

		mx = 1;
		callout("printf", "max = %d; ", mx);

		for i=0, 2 {
			if (true) {
				int az;
				mx = 2;
				callout("printf", "max = %d; ", mx);
			}
		}

		if (temp(3, 0) > 2) {
			callout("printf", "true; ");
		}
		else {
			callout("printf", "false; ");	
		}

		for i=temp(2, 0), 5 {
			int aa;
			callout("printf", "i = %d; ", i);
		}
		
		return 0;
	}
}
