int solve(int n, int s, int t, int a) {
	if (n == 1) Print("disk 1: ", s, " -> ", t);
	else {
		solve(n-1, s, a, t);
		Print("disk ", n, ": ", s, " -> ", t);
		solve(n-1, a, t, s);
	}
	return(0);
}

int main(void) {
	int n; ReadInteger(n);
	solve(n, 1, 3, 2);
}