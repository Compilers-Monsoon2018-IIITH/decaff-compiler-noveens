class Program {
	int global_int;
	int global_arr[100];
	boolean global_bool;

	int main() {
		int a, b;
		a = 1;
		
		if (a + 5) {} // shouldn't work

		if (a + 5 >= 1) {} // should work

		return 0;
	}	
}