class Program {
	int global_int;
	int global_arr[100];
	boolean global_bool;

	int main() {
		int a, b;
		bool c;

		for i=true, 5 {} // shouldn't work
		for i=0, true {} // shouldn't work
		for i=0, 5 {} // should work

		return 0;
	}	
}