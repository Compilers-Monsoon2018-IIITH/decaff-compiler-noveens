int main(void) {
	int n, i; ReadInteger(n);
	int[] arr;
	arr = NewArray(n, int);

	for (i=0;i<n;i++) {
		int temp; ReadInteger(temp);
		arr[i] = temp;
	}

	for (i=0;i<n;i++) {
		if (arr[i] % 2 == 0) {
			Print(arr[i], " ");
		}
	}
	Print("\n");

	return(0);
}