class Program {
	int [ 100000 ] arr;

	int main() {
		int n, i; ReadInteger(n);
		
		for i=0,i<n {
			int temp; ReadInteger(temp);
			arr[i] = temp;
		}

		for i=0,i<n {
			if (arr[i] % 2 == 0) {
				Print(arr[i], " ");
			}
		}
		Print("\n");

		return 0;
	}
}